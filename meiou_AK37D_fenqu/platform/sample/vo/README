字符图层显示说明：
字符存放在在anyka_monobmp.h，
有效的字符包括：ascii编码的字符；13个中文字符。
字符的点阵是按照一个像素一个bit的mono点整
每个点阵信息都包含在一个数组中说明。
ascii的字符点阵是按照半角存储，像素大小为32*16（w*h)
中文字符是32*32

使用vo显示字符的流程：
1.由于创建的图层是rgb格式的图层，即rgb888，一个像素3个byte，首先需要将mono的点阵转化成rgb888格式，并贴到图层的对应位置上；
2.向一个bit一个像素的位图，转化为3byte一个像素的位图（直接填到图层上）
3.按照画出的字符数量，行数和行间距，在图层上做相应的偏移；

其中有几个参数设置，第一个
-d [num] 指定需要画出的行数；
-m [mode] 指出需要做的操作，0-colorky 1-alpha；

编译：将该目录直接放到/platform/sample/ 下，替换vo的sample目录，直接编译就可以了

注意事项：
1.vo的sample需要有一些图片来显示才会看出现效果，在pic_data中有一张yuv420p的数据图片；
2.logo已经包含在pic_data文件夹中了，可以直接指定
